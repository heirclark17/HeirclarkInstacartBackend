# Port for local dev; Railway sets PORT automatically in prod
PORT=3000

# Frontend(s) allowed to call your non-proxy API endpoints
ALLOWED_ORIGINS=https://heirclark.com

# From Shopify App → App setup → App credentials → API secret key
SHOPIFY_API_SECRET=YOUR_SHOPIFY_APP_SECRET

# Must match your App Proxy public path exactly
# (Partners > App > App setup > App proxy: prefix=apps, subpath=heirclark)
SHOPIFY_PROXY_PATH=/apps/heirclark
